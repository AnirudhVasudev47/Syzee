import 'package:flutter/material.dart';

class BrandTab extends StatelessWidget {
  const BrandTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Brand Tab'),
    );
  }
}
